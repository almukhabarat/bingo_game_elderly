import sys
import os

# Add the app's directory to the sys.path
sys.path.insert(0, '/home/ti3groep/stepper_flask_api')

from vendingmachine_api import app as application  # Import the app instance from vendingmachine_api

