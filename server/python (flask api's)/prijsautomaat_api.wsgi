import sys
import os

# Add the app's directory to the sys.path
sys.path.insert(0, '/home/ti3groep/control_flask_api')

from prijsautomaat_api import app as application  # Import the app instance from prijsautomaat_api

