import sys
import os

# Add the app's directory to the sys.path
sys.path.insert(0, '/home/ti3groep/control_flask_api')

from bingobal_api import app as application  # Import the app instance from bingobal_api

